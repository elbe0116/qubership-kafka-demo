{{- if and .Values.integrationTests.install .Values.integrationTests.atpReport.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.integrationTests.service.name }}-atp-storage-secret
  labels:
    {{- include "kafka-services.defaultLabels" . | nindent 4 }}
type: Opaque
stringData:
  atp-storage-username: {{ .Values.integrationTests.atpStorage.username }}
  atp-storage-password: {{ .Values.integrationTests.atpStorage.password }}
{{- end }}
